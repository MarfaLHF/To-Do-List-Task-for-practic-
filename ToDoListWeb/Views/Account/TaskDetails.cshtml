@model ToDoList.Data.Models.Task

<div class="container mt-5">
    <h1>Детали задачи</h1>
    <form asp-controller="Account" asp-action="UpdateTask" method="post">
        <div class="form-group row mb-3">
            <label class="col-sm-2 col-form-label">Название:</label>
            <div class="col-sm-10">
                <input type="text" asp-for="Title" class="form-control" />
            </div>
        </div>

        <div class="form-group row mb-3">
            <label class="col-sm-2 col-form-label">Описание:</label>
            <div class="col-sm-10">
                <textarea asp-for="Description" class="form-control"></textarea>
            </div>
        </div>

        <div class="form-group row mb-3">
            <label class="col-sm-2 col-form-label">Время выполнения:</label>
            <div class="col-sm-10">
                <input type="datetime-local" asp-for="Deadline" class="form-control" />
            </div>
        </div>

        <div class="form-group row mb-3">
            <label class="col-sm-2 col-form-label">Приоритет:</label>
            <div class="col-sm-10">
                <select asp-for="Priority" class="form-control">
                    <option value="1">Высокий</option>
                    <option value="2">Средний</option>
                    <option value="3">Низкий</option>
                </select>
            </div>
        </div>

        <div class="form-group row mb-3">
            <div class="col-sm-2">Статус:</div>
            <div class="col-sm-10">
                <input type="checkbox" asp-for="IsCompleted" />
            </div>
        </div>

        <div class="form-group row mb-3" style="visibility:hidden">
            <dt class="col-sm-2">Статус:</dt>
            <dd class="col-sm-10">
                <input asp-for="UserId" />
            </dd>
        </div>

        <input type="hidden" asp-for="Id" />
        <div class="mb-3">
            <button type="submit" class="btn btn-primary btn-block">Сохранить изменения</button>
        </div>
    </form>

    <form asp-controller="Account" asp-action="DeleteTask" method="post" onsubmit="return confirm('Вы уверены, что хотите удалить задачу?');">
        <input type="hidden" asp-for="Id" />
        <input type="hidden" name="userId" value="@ViewData["userId"]" />
        <div class="mt-3">
            <button type="submit" class="btn btn-danger btn-block">Удалить задачу</button>
        </div>
    </form>
</div>
